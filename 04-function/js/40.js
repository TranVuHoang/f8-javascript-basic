/**
 * Hàm (function) trong javasctipt
 * 
 * 1. Hàm?
 *      - Một khối mã
 *      - làm 1 việc cụ thể
 * 2. Loại hàm
 *      - Built-in
 *      - tự định nghĩa
 * 3. Tính chất
 *      - Không thực thi khi định nghĩa
 *      - Sẽ thực thi khi được gọi
 *      - Có thể trả về một giá trị
 * 4. Tạo hàm đầu tiên
 */

// khởi tạo hàm(tạo hàm)
function showDialog() {
    alert("hi xin chào các bạn");
}

// gọi hàm
showDialog();
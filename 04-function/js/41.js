// "tham số" (parameter) và "đối số" (argument) của một hàm là:
// - Tham số: là các ký hiệu thuộc phần ĐỊNH NGHĨA của hàm.
// - Đối số: là các ký hiệu ĐƯỢC CUNG CẤP cho hàm khi nó được dùng.
function showMessage(message) {// tham số message
    console.log(message);
}

showMessage("Hi anh em F8!"); // đối số truyền vào: Hi anh em F8! 
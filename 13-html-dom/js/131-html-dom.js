/** HTML DOM là gì?
 *  DOM: Document Object Model
 *      - Mô hình tài liệu được thể hiện dưới dạng đối tượng]
 *      - trang web được tải thì trình duyệt sẽ tạo 1 DOM
 *      - trình duyệt sẽ theo quy chuẩn w3school để đọc tài liệu HTML
 *      - tạo ra một mô hình DOM dạng cây
 *      - cây này sẽ được trình duyệt đọc
 *      - DOM có 3 thành phần :
 *          -1. Elements(có bao nhiêu thẻ(tag) thì có bấy nhiêu emlement)
 *          -2. Attribute(thuộc tính của thẻ mở HTML)
 *          -3. Text 
 *      - Mỗi ô hình vuông trong cây DOM là 1 Node(điểm giao nhau, điểm gồ lên trong cây)
 *  - có thể thay đổi DOM -> thay đổi cả website của chúng ta
 *  - 3 tiêu chuẩn w3school:
 *      - core DOM: tạo quy chuẩn cho tất cả tài liệu
 *      - XML DOM : quy chuẩn tài liệu XML
 *      - HTML DOM: quy chuẩn cho tài liệu HTML
 * - => sử sụng HTML DOM: có thể thay đổi toàn bộ những j trên trang web
 *  
 */

/** HTML DOM vs DOM API
 *  Phân định giữa HTML DOM và ngôn ngữ Javascript
 * --------------------
 * 
 * - HTML là quy chuẩn của w3chool cho website.
 * - Javascript cung cấp các đối tượng để truy xuất vào mô hình HTML DOM
 * => HTML DOM k phải là javascript
 * 
 * javascript chạy trên browser(trình duyệt), server(nodejs)
 * API: Application programming Interface:
 *       web API chỉ chạy trên môi trường web
 * Browser: HTML -> DOM -> DOM API
 * 
 */